export class ResourceType {
    public spritekey: string;
    public value: number;

    constructor(spriteKey: string, value: number) {
        this.spritekey = spriteKey;
        this.value = value;
    }
}
